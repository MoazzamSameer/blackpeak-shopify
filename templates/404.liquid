<section class="section text-center">
  <div class="container">
    <p class="eyebrow">404</p>
    <h1 class="h1 display">{{ 'general.404.title' | t }}</h1>
    <p class="lede">{{ 'general.404.body' | t }}</p>
    <a class="btn btn--saffron btn--lg" href="{{ routes.all_products_collection_url }}">{{ 'general.404.link' | t }}</a>
  </div>
</section>
