<section class="section">
  <div class="container">
    <div class="section__head">
      <h1 class="h1 display">{{ page.title }}</h1>
    </div>
    {%- if page.content != blank -%}
      <div class="lede">{{ page.content }}</div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{ "name": "Page content", "settings": [] }
{% endschema %}
